"use client";
import { useState } from "react";
import Link from "next/link";
import { createSupabaseBrowserClient } from "@/lib/supabase/browser";
export default function ResetPassword(){const[password,setPassword]=useState("");const[message,setMessage]=useState("");const[failed,setFailed]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();try{const{error}=await createSupabaseBrowserClient().auth.updateUser({password});setFailed(Boolean(error));setMessage(error?"No se pudo actualizar el acceso.":"Contraseña actualizada. Ya podés ingresar.");}catch{setFailed(true);setMessage("Supabase no está configurado en este entorno.");}}return <main className="min-h-screen grid place-items-center p-5"><section className="card p-8 max-w-md w-full"><p className="eyebrow">Seguridad</p><h1 className="text-2xl font-black mt-2">Definí una nueva contraseña</h1><form onSubmit={submit} className="grid gap-4 mt-6"><label className="label">Nueva contraseña<input className="field" type="password" minLength={12} value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="btn btn-primary">Actualizar contraseña</button></form>{message&&<p className={`rounded-xl p-3 text-sm mt-4 ${failed?"bg-red-50 text-red-800":"bg-[var(--brand-soft)] text-[var(--brand-strong)]"}`}>{message}</p>}<Link href="/login" className="block text-center text-sm font-bold text-[var(--brand)] mt-5">Volver al ingreso</Link></section></main>}
